import { createBrowserHistory } from 'history'; // function we can call to create a new history object

export default createBrowserHistory(); // invokes above function and creates history object we can access anywhere in our project